#
# LDAP Defaults
#
#
# See ldap.conf(5) for details
# This file should be world readable but not world writable.
#
BASE                dc=cgcs,dc=local
URI                 ldap://<%= @ldapserver_host %>
pam_lookup_policy   yes
sudoers_base        ou=SUDOers,dc=cgcs,dc=local
