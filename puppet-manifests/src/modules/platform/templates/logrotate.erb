<%= @log_file_name %>
{
    size <%= @log_file_size %>
    rotate <%= @log_file_rotate %>
    start 1
    missingok
    compress
    sharedscripts
    nodateext
    postrotate
        systemctl reload syslog-ng > /dev/null 2>&1 || true
    endscript
}

